[project]
name = "documentctl"
version = "0.1.0"
description = "Desktop PDF search engine with a C trie indexer and a PyQt reader"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pymupdf>=1.24.0",
    "pyqt6>=6.6.0",
]
